# Development

- Use Node.js 26 and pnpm. Install dependencies with `pnpm install`.
- Run tests with `pnpm test`.
- Run type checking with `pnpm typecheck`.
- Validate CLI changes with `pnpm start --help` and relevant commands, such as `pnpm start load toggl.csv`.
